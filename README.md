# Doc

# Create virtual env
Launch this just one

```python3 -m venv .venv```

# Activate on Linux

```source .venv/bin/activate```

# Activate on windows

```.\.venv\bin\activate```

# Install requirements

```pip install -r requirements.txt```